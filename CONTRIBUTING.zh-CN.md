# 贡献指南

感谢您对为 figma-pilot 做出贡献感兴趣！本文档提供了贡献的指南和说明。

## 行为准则

通过参与此项目，您同意遵守我们的[行为准则](./CODE_OF_CONDUCT.md)。

## 我如何贡献？

### 报告 Bug

在创建 bug 报告之前，请检查问题列表，因为您可能会发现不需要创建一个。创建 bug 报告时，请尽可能包含详细信息：

- **使用清晰且描述性的标题**
- **描述重现问题的确切步骤**
- **提供具体示例来演示步骤**
- **描述遵循步骤后观察到的行为**
- **解释您期望看到的行为以及原因**
- **如果适用，包括屏幕截图和动画 GIF**
- **包括系统信息**（操作系统、Node.js 版本、Figma 版本等）

### 建议增强功能

增强功能建议作为 GitHub 问题进行跟踪。创建增强功能建议时，请包括：

- **使用清晰且描述性的标题**
- **提供建议增强功能的逐步描述**
- **提供具体示例来演示步骤**
- **描述当前行为并解释您期望看到的行为**
- **解释为什么此增强功能会有用**

### 拉取请求

- 填写所需的模板
- 不要在 PR 标题中包含问题编号
- 尽可能在拉取请求中包含屏幕截图和动画 GIF
- 遵循 TypeScript 和代码风格指南
- 包含经过深思熟虑、结构良好的测试
- 根据文档风格指南记录新代码
- 所有文件以换行符结尾

## 开发设置

### 前置要求

- Node.js >= 18
- Bun（推荐）或 npm/yarn
- Figma Desktop 应用
- Git

### 开始使用

1. **Fork 仓库**

2. **克隆您的 fork：**
   ```bash
   git clone https://github.com/YOUR_USERNAME/figma-pilot.git
   cd figma-pilot
   ```

3. **安装依赖：**
   ```bash
   bun install
   ```

4. **构建项目：**
   ```bash
   bun run build
   ```

5. **设置开发环境：**
   - 从 Figma Desktop 中的 `packages/plugin/manifest.json` 导入插件
   - 本地运行 MCP 服务器：`bun run packages/mcp-server/dist/index.js`
   - 或使用 CLI：`bun run cli status`

### 开发工作流

1. 从 `master` 创建分支：
   ```bash
   git checkout -b feature/your-feature-name
   ```

2. 进行更改

3. 测试您的更改：
   ```bash
   # 构建
   bun run build
   
   # 运行测试（如果可用）
   # bun test
   
   # 在 Figma 中测试插件
   # 使用您的 AI 客户端测试 MCP 服务器
   ```

4. 提交您的更改：
   ```bash
   git add .
   git commit -m "feat: 添加您的功能描述"
   ```

5. 推送到您的 fork：
   ```bash
   git push origin feature/your-feature-name
   ```

6. 在 GitHub 上创建拉取请求

## 编码标准

### TypeScript

- 所有新代码使用 TypeScript
- 遵循现有的代码风格
- 使用有意义的变量和函数名
- 为公共 API 添加 JSDoc 注释
- 优先使用 `const` 而不是 `let`，避免 `var`

### 代码风格

- 使用 2 个空格缩进
- 字符串使用单引号（除非需要转义）
- 多行对象/数组使用尾随逗号
- 最大行长度：100 个字符
- 使用分号

### 文件结构

- 保持文件专注且小巧
- 将相关功能分组
- 使用描述性的文件名
- 将共享类型放在 `packages/shared`

### 提交消息

遵循 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

- `feat:` 用于新功能
- `fix:` 用于 bug 修复
- `docs:` 用于文档更改
- `style:` 用于格式更改
- `refactor:` 用于代码重构
- `test:` 用于添加测试
- `chore:` 用于维护任务

示例：
```
feat: 添加渐变填充支持
fix: 修正文本对比度的无障碍性检查
docs: 使用新安装步骤更新 README
```

## 项目结构

```
figma-pilot/
├── packages/
│   ├── cli/              # CLI 应用程序
│   │   └── src/
│   │       ├── commands/ # CLI 命令
│   │       └── utils/    # CLI 实用程序
│   ├── plugin/           # Figma 插件
│   │   └── src/
│   │       ├── handlers/ # 请求处理程序
│   │       └── utils/    # 插件实用程序
│   ├── mcp-server/       # MCP 服务器
│   │   └── src/
│   │       └── index.ts  # MCP 服务器入口
│   └── shared/           # 共享类型
│       └── src/
│           ├── types.ts  # TypeScript 类型
│           └── operations.ts # 操作定义
├── scripts/              # 构建和实用程序脚本
└── .github/             # GitHub 模板和工作流
```

## 测试

添加新功能或修复 bug 时：

1. **在 Figma Desktop 中测试：**
   - 从 `packages/plugin/manifest.json` 导入插件
   - 测试所有受影响的操作
   - 验证错误处理

2. **使用 MCP 客户端测试：**
   - 使用 Claude Desktop/Code 测试
   - 使用 Cursor 测试
   - 验证工具架构是否正确

3. **测试边缘情况：**
   - 无效输入
   - 缺少选择
   - 网络错误
   - 插件断开连接

## 文档

- 如果添加新功能，更新 README.md
- 如果添加新的 MCP 工具，更新 SKILL.md
- 为公共 API 添加 JSDoc 注释
- 在文档中包含示例

## 发布流程

发布由维护者管理。当发布准备就绪时：

1. 更新 `package.json` 文件中的版本号
2. 更新 CHANGELOG.md
3. 创建 git 标签
4. 构建并发布到 npm
5. 创建 GitHub 发布

## 有问题？

如果您有问题，请：
- 打开带有 `question` 标签的问题
- 检查现有问题和讨论
- 查看文档

感谢您为 figma-pilot 做出贡献！🎉
